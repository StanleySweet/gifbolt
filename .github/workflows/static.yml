# DEPRECATED: Use docs.yml instead
# This workflow is disabled in favor of docs.yml which properly builds and deploys documentation
name: Deploy static content to Pages (DISABLED)

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "::notice::This workflow is disabled. Use the 'Build & Deploy Documentation' workflow (docs.yml) instead."
          exit 1
